<?xml version="1.0" encoding="utf-8"?><manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="teamplaceholder.com.placeholderapp">

<application android:allowBackup="true" android:icon="@mipmap/ic_water" android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/Theme.AppCompat.Light.NoActionBar">
    <activity android:name=".Controller.MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity android:name=".Controller.LoginActivity" />
    <activity android:name=".Controller.HomeActivity" />
    <activity android:name=".Controller.RegisterNewUserActivity" />
    <activity android:name=".Controller.EditActivity"></activity>
    <activity android:name=".Controller.SubWaterRepoActivity"></activity>
</application>
</manifest>
